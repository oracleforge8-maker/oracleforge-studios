# ============================================================
# OracleForge Studios — Render.com Procfile
# Tells the platform how to run the app in production.
# ============================================================

# Production: Gunicorn serves the combined web+dashboard app
web: gunicorn wsgi:app --config gunicorn.conf.py